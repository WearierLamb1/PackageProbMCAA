#Ibstalar primero RTools

#Comando necesarios para instalar los paquetes y dependencias
#Dependencias
install.packages("timeDate", dependencies = TRUE)
install.packages("timeSeries", dependencies = TRUE)
install.packages("timefBasics", dependencies = TRUE)
install.packages("timefOptions", dependencies = TRUE)

#Paquetes 
install.packages("C:/ruta al documento", repos = NULL, type = "source")

#Paqueterias 
fAsianOptions
prob

#La instalación debe de hacerse desde la consola de RStudio